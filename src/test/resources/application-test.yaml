spring:
  datasource:
    url: "jdbc:tc:mysql:8.0:///coongyapay?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&sessionVariables=time_zone='%2B09:00'"
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  sql:
    init:
      mode: always
      continue-on-error: false
  session:
    store-type: none

# 테스트는 스케줄 타이머 대신 ChargeProcessor.process()를 직접 호출한다.
charge:
  worker:
    enabled: false
